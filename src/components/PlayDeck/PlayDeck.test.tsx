import React from "react";
// import { shallow } from "enzyme";
import PlayDeck from "./PlayDeck";

describe("PlayDeck", () => {
  test("matches snapshot", () => {
    // const wrapper = shallow(<PlayDeck />);
    // expect(wrapper).toMatchSnapshot();
  });
});
