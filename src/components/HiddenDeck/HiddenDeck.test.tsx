import React from "react";
import HiddenDeck from "./HiddenDeck";

describe("HiddenDeck", () => {
  test("matches snapshot", () => {
    // const wrapper = shallow(<HiddenDeck />);
    // expect(wrapper).toMatchSnapshot();
  });
});
